console.log('Hello world');
const userId = 1234;
// create UserCtrl
// initialize the form with user data got from the UserService
// on save button call the update method of the UserService


// create UserService having two methods:
// getOne() returning Promise of User data (1 s delay)
// update() returning Promise and logging the updated user to the console (1 s delay)

